---
title: "R Notebook"
output: html_notebook
---


```{r}
library(dplyr)
library(ggplot2)# Load the ape package
library(ape)
library(readxl)
library(tidyr)
library(ggpubr)
library(ade4)
library(vegan)
library(stringr)
library(brms)
library(dplyr)
library(tidyverse)
library(cmdstanr)
```



```{r}
df_habitat <- read.csv("df_habitat")

df_load <- read.csv("df_load")

```



Input data

```{r}


data_list <- list(
  
  # N rows
  
  N_nem = length(df_load$nematode_count),
  N_hab = length(df_habitat$habitat),  # N rows for habitat or N unique habitats (3)???
  
  # N species
  
  N_sp_nem = length(unique(df_load$species)),
  N_sp_hab = length(unique(df_habitat$species)),
  
  # y
  
  load = df_load,
  
  # x
  
  hab = df_habitat$habitat,
  
  # species id
  
  species_i = df_load$species_code,
  species_k = df_habitat$species_code
  
)

```